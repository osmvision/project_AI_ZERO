# Example env file for docker-compose
# Copy to .env and edit values

# If you want to use the prebuilt image in docker-compose.prod.yml
# set IMAGE to your GHCR or Docker Hub image (e.g. ghcr.io/youruser/zeroai:latest)
IMAGE=ghcr.io/<OWNER>/zeroai:latest

# Embedding model to use (default in code is sentence-transformers/all-MiniLM-L6-v2)
EMBED_MODEL=sentence-transformers/all-MiniLM-L6-v2
EMBED_DEVICE=cpu

# Whisper model for S2T (tiny/base/small/medium)
S2T_MODEL=tiny

# If using private HuggingFace models, set this token
# HUGGINGFACE_HUB_TOKEN=
